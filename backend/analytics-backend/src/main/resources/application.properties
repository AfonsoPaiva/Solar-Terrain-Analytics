spring.application.name=analytics-backend
server.port=8081
# H2/JPA config removed (using Firestore). Ensure FIREBASE_KEY_PATH is set.
# Optionally set path here instead of environment variable:
# firebase.key.path=C:/Users/afons/AppData/Local/SolarTerrain/serviceAccount.json

# Logging for debug
logging.level.com.solarterrain.analytics_backend=DEBUG
logging.level.org.springframework.security=INFO
